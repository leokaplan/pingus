#include "sdl.ceu"
input void SDL_REDRAW;
var _SDL_Rect quad;
    quad.x = 100;
    quad.y = 100;
    quad.w = 50;
    quad.h = 50;

var _SDL_Color color;
    color.r = 0xFF;
    color.g = 0x00;
    color.b = 0x00;

every SDL_REDRAW do
    call {Display::get_framebuffer()->fill_rect}({Rect(0, 0, 50, 60)}, {Color(155, 0, 155)})  ;//finalize with nothing; end;
end
